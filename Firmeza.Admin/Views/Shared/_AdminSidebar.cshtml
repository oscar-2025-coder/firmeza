@{
    var current = (string)ViewContext.RouteData.Values["Controller"]!;
}
<div class="p-3">
    <h5 class="mb-4 fw-semibold text-white">Firmeza Admin</h5>
    <ul class="nav nav-pills flex-column gap-1">
        <li class="nav-item">
            <a class="nav-link d-flex align-items-center gap-2 @(current == "Admin" ? "active" : "text-white-50")"
               asp-controller="Admin" asp-action="Index">
                🏠 <span>Dashboard</span>
            </a>
        </li>

        <li class="nav-item">
            <a class="nav-link d-flex align-items-center gap-2 @(current == "Products" ? "active" : "text-white-50")"
               asp-controller="Products" asp-action="Index">
                📦 <span>Products</span>
            </a>
        </li>

        <li class="nav-item">
            <a class="nav-link d-flex align-items-center gap-2 @(current == "Customers" ? "active" : "text-white-50")"
               asp-controller="Customers" asp-action="Index">
                👥 <span>Customers</span>
            </a>
        </li>

        <li class="nav-item">
            <a class="nav-link d-flex align-items-center gap-2 @(current == "Sales" ? "active" : "text-white-50")"
               asp-controller="Sales" asp-action="Index">
                💵 <span>Sales</span>
            </a>
        </li>

        <!-- 👉 Nueva opción para la Task 8: carga masiva desde Excel -->
        <li class="nav-item">
            <a class="nav-link d-flex align-items-center gap-2 @(current == "DataImport" ? "active" : "text-white-50")"
               asp-controller="DataImport" asp-action="BulkImport">
                📥 <span>Bulk Import</span>
            </a>
        </li>
    </ul>
</div>

<style>
    .nav-pills .nav-link {
        border-radius: .5rem;
        transition: all .2s ease;
    }

    .nav-pills .nav-link.active {
        background: #0d6efd;
        color: #fff !important;
        font-weight: 600;
    }

    .bg-dark .nav-link.text-white-50:hover {
        color: #fff !important;
    }
</style>
