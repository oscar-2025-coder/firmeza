@* Validation scripts are loaded globally in _AdminLayout.cshtml.
   This file is intentionally left empty to avoid duplicate loading
   and to prevent jQuery from failing before validation scripts load. *@
